function commutation(str) {
    if (str) {
        document.write("<hr>");
        document.write("===" + str + "===");
        document.write("<br>");
    }
    else {
        document.write("<br>");
    }
}